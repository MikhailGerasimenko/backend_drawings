"""Жёсткие правила маршрута — дописываются к system после промпта команды и APPENDIX."""

TECHNOLOGY_USER_OPERATIONS_RULES = """КРИТИЧЕСКИЕ ПРАВИЛА МАРШРУТА (обязательны, дописываются в system после APPENDIX):
1. Маршрут (поле route) ДОЛЖЕН содержать ТОЛЬКО операции из блока «Выбранные операции», переданного пользователем.
2. Запрещено добавлять операции, станки/оборудование или переходы, которых нет в этом списке.
3. Допускается задавать порядок OP, переходы и итоговые размеры только в рамках выбранных пар «операция — оборудование».
4. Общие правила из системного промпта (припуски, ТО, шероховатость, контроль размеров) применяй для расчётов, но не для расширения перечня операций.
5. Если для полного маршрута не хватает данных — укажи это в confirmation_required, не добавляя новые операции."""

# Пропуск ручного выбора: маршрут и оборудование — только из справочника в system (см. OPERATIONS_CATALOG_SYSTEM_APPENDIX)
TECHNOLOGY_CATALOG_OPERATIONS_RULES = """КРИТИЧЕСКИЕ ПРАВИЛА МАРШРУТА (пропущен ручной выбор операций):
1. Поле route MUST строиться только из справочника операций команды (блок в system выше).
2. Запрещено добавлять операции, станки/оборудование или переходы, которых нет в справочнике.
3. Порядок OP, переходы и размеры — в рамках выбранных из справочника пар «операция — оборудование».
4. Общие правила системного промпта (припуски, ТО, шероховатость) применяй для расчётов, но не для расширения справочника.
5. Если данных не хватает — confirmation_required, без операций вне справочника."""
