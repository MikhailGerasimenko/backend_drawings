[project]
name = "fastapi-template"
version = "0.1.1"
description = ""
authors = [
    {name = "Alexander Gvozdev",email = "ad.gvozdev@severstal.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi (>=0.128.0,<0.129.0)",
    "uvicorn (>=0.40.0,<0.41.0)",
    "gunicorn (>=23.0.0,<24.0.0)",
    "pydantic-settings (>=2.12.0,<3.0.0)",
    "pytz (>=2025.2,<2026.0)",
    "opentelemetry-distro (>=0.48b0,<1.0.0)",
    "opentelemetry-exporter-otlp (>=1.28.0,<2.0.0)",
    "opentelemetry-instrumentation-fastapi (>=0.48b0,<1.0.0)",
    # Database
    "sqlalchemy (>=2.0.36,<3)",
    "psycopg2-binary (>=2.9.10,<3)",
    "alembic (>=1.14.0,<2)",
    # File handling
    "python-multipart (>=0.0.12,<1)",
<<<<<<< HEAD
    # Excel technology card export
    "openpyxl (>=3.1.0,<4)",
=======
>>>>>>> gitlab/dev
    # PDF generation
    "reportlab (>=4.2.0,<5) ; python_version<'3.14'",
    "pymupdf (>=1.24.0,<2) ; python_version<'3.14'",
    # Monitoring
    "sentry-sdk[fastapi] (>=2.19.0,<3)",
    # Security / Auth
    "passlib[bcrypt] (>=1.7.4,<2)",
    "python-jose[cryptography] (>=3.3.0,<4)",
    # Async database driver
    "asyncpg (>=0.29.0,<1)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "isort (>=7.0.0,<8.0.0)",
    "black (>=26.1.0,<27.0.0)",
    "pytest (>=8.0.0,<9.0.0)",
    "pytest-cov (>=5.0.0,<6.0.0)",
    "pytest-mock (>=3.14.0,<4.0.0)",
    "pytest-asyncio (>=0.24.0,<1.0.0)",
    "httpx (>=0.28.1,<0.29.0)"
]

[[tool.poetry.source]]
name = "corporate"
url = "https://repo.severstal.severstalgroup.com/artifactory/api/pypi/pypi/simple"
priority = "primary"

[tool.isort]
line_length = 79
multi_line_output = 3
include_trailing_comma = true
use_parentheses = true
known_first_party = ["app"]
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]
force_sort_within_sections = true

[tool.black]
line-length = 79
target-version = ["py311"]
exclude = '''
/(
    \.git
  | \.venv
  | \.mypy_cache
  | \.pytest_cache
  | \.ruff_cache
  | build
  | dist
)/
'''

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
]