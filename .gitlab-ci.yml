include:
  - project: '${SSS_V3_CI_TEMPLATE_PROJECT}'
    file:
      - '/python.yml'

variables:
  SKIP_PUBLISH: "true"
  DEPLOYMENT_ENVIRONMENT: dev
  
deploy:
  before_script:
    - kubectl -n ${NAMESPACE} delete svc ${CI_PROJECT_NAME_SLUG} --ignore-not-found=true || true
